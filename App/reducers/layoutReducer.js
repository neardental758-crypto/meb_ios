

const initialState = {

};

export default layoutReducer = (state = initialState, action) => {
    switch (action.type) {
        /* case FETCH_DEVICE_CONFIGURATION_BEGIN:
            return {
                ...state,
                data: [],
                isFetching: true
            } */
        default:
            return state;
    }
}